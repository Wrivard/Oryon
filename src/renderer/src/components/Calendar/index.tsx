// Vue Calendar (déclenchée par l'entrée « Calendar » du rail, au-dessus des workspaces).
// PLACEHOLDER DE CONTRAT (Phase 0) : le lot B remplace le CORPS par le vrai calendrier
// (react-big-calendar, vues jour/semaine/mois, thème Oryon via skills design, events via
// window.bridge.calendar.events). ⚠ Le lot A importe ce module — NE CHANGE PAS le nom du
// named export `CalendarView` ni le fait qu'il est propless.
export function CalendarView(): JSX.Element | null {
  return null
}
